# ESMValTool
#DASK --memory_limit=7760MB
#DASK --n_workers=4
#TAGS ocean
#TAGS timeseries
---
documentation:
  title: Ocean Evaluation (Time Series).
  description: Time series of ocean variables.
  authors:
    - bock_lisa
    - weigel_katja


# YAML anchors for later use

datasets_cmip6: &datasets_cmip6
  - {project: CMIP6, dataset: "*", exp: historical, ensemble: r1i1p1f1, grid: gn, institute: "*", timerange: 19790101/20141231}  # 18500101/20141231

datasets_tos: &datasets_tos
  - {project: OBS6, dataset: ESACCI-SST, type: sat, version: 3.0-L4-analysis, tier: 2, timerange: 19800101/20211231}  # 19800101/20211231
  - {project: OBS, dataset: HadISST, type: reanaly, version: "1", tier: 2, timerange: 19790101/20211231}  # 18700101/20211231
  - {project: OBS6, dataset: NOAA-ERSSTv5, type: reanaly, version: v5, tier: 2, timerange: 19790101/20201231}  # 18540101/20201231

supplementary_variables: &supplementary_variables
  - {short_name: areacella, skip: true}
  - {short_name: areacello, skip: true}
  - {short_name: sftlf, skip: true}
  - {short_name: sftof, skip: true}

plot_script: &plot_script
  script: monitor/multi_datasets.py
  facet_used_for_labels: alias
  figure_kwargs:
    figsize: [7, 5]
  group_variables_by: variable_group
  plot_filename: "{plot_type}_{exp}_{real_name}_{dataset}_{mip}"
  plot_folder: "{plot_dir}"

timeseries_plot: &timeseries_plot
  gridline_kwargs:
    color: darkgray
    linewidth: 0.5
    alpha: 0.3
  legend_kwargs:
    loc: upper center
    bbox_to_anchor: [0.5, -0.4]
    borderaxespad: 0.0
  pyplot_kwargs:
    title: "{title}"
  plot_kwargs:
    "{{alias_plot_kwargs}}":
      label: "{dataset} ({exp})"
      linewidth: 1.25
      zorder: 2.5
    default:  # These will be used for CMIP6 models
      color: lightgray
      label: null
      linewidth: 0.75
      zorder: 1.0
    CMIP6:
      label: "{project}"  # just to have one label for the gray lines
    CMIP6_CESM2:
      label: "{project}"  # just to have one label for the gray lines
    OBS: &obs_format
      color: black
      label: "{dataset}"
      linewidth: 1.0
      zorder: 2.4
    OBS6: &obs6_format
      <<: *obs_format
      linestyle: dashdot
    native6: &native6_format
      <<: *obs_format
      linestyle: dotted
    OBS_HadISST: *obs_format
    OBS6_ESACCI-SST:
      <<: *obs_format
      linestyle: dashed
    OBS6_NOAA-ERSSTv5: *obs6_format


datasets: "{{dataset_list}}"


preprocessors:
  create_global_annual_mean_time_series:
    custom_order: true
    regrid:
      target_grid: 2x2
      scheme: linear  # some models do not work with ESMFAreaWeighted
    area_statistics:
      operator: mean
    annual_statistics:
      operator: mean
    convert_units:
      units: degC

  create_nino34_time_series:
    custom_order: true
    regrid:
      target_grid: 2x2
      scheme: linear  # some models do not work with ESMFAreaWeighted
    extract_region:
      start_longitude: -170
      end_longitude: -120
      start_latitude: -5
      end_latitude: 5
    mask_landsea:
      mask_out: land
    anomalies:
      period: month
    rolling_window_statistics:
      coordinate: time
      operator: mean
      window_length: 3
    area_statistics:
      operator: mean


diagnostics:

  timeseries_nino34:
    description: Time series for Oceanic Nino Index in Nino 3.4 region.
    themes:
      - phys
    realms:
      - ocean
    variables:
      tos:
        mip: Omon
        preprocessor: create_nino34_time_series
        title: Nino 3.4
        additional_datasets: *datasets_tos
        supplementary_variables: *supplementary_variables
    additional_datasets: *datasets_cmip6
    scripts:
      plot:
        <<: *plot_script
        plots:
          timeseries:
            <<: *timeseries_plot
            hlines:
              - y: -1.5
                color: darkgray
                linestyle: dashed
                linewidth: 1.0
              - y: 1.5
                color: darkgray
                linestyle: dashed
                linewidth: 1.0
            pyplot_kwargs:
              title: "{title}"
              ylim: [-3, 3]
              ylabel: "Oceanic Nino Index (K)"

  timeseries_tos:
    description: Time series for global mean sea surface temperature.
    themes:
      - phys
    realms:
      - ocean
    variables:
      tos:
        mip: Omon
        preprocessor: create_global_annual_mean_time_series
        title: Global Mean Sea Surface Temperature
        additional_datasets: *datasets_tos
        supplementary_variables: *supplementary_variables
    additional_datasets: *datasets_cmip6
    scripts:
      plot:
        <<: *plot_script
        plots:
          timeseries:
            <<: *timeseries_plot
